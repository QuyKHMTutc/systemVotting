package com.xxxx.systemvotting.modules.poll.enums;

public enum PollVisibility {
    PUBLIC,   // Hiển thị công khai cho tất cả mọi người
    PRIVATE   // Chỉ những người được mời (qua email) mới có thể xem và bình chọn
}
