# Tên ?ng d?ng và Port c? ??nh
spring.application.name=SystemVotting

server.port=8080

# C?u hình JWT (Th?i gian h?t h?n th??ng c? ??nh)
app.security.jwt.expiration=900000

app.security.jwt.refresh-token.expiration=604800000

# C?u hình Mail chung (Host và Port c?a Gmail th??ng không ??i)
spring.mail.host=smtp.gmail.com
spring.mail.port=587

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Actuator (C?u hình chung ?? theo dõi h? th?ng)
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always

# Dòng này ?? ch?n môi tr??ng m?c ??nh khi b?n ?n Run trong IDE
spring.profiles.active=dev