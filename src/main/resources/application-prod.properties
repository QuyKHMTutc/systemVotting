# Database trên Server (URL có th? khác localhost)
spring.datasource.url=jdbc:mysql://production-db-server:3306/systemvotting
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# T?i ?u Hibernate cho Production (T?t hi?n SQL ?? b?o m?t và hi?u n?ng)
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# Các thông tin nh?y c?m b?t bu?c l?y t? môi tr??ng
app.security.jwt.secret-key=${JWT_SECRET_KEY}
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}